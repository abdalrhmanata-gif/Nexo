name: nexo_followthrough
description: NEXO Follow-through Engine MVP
publish_to: "none"
version: 1.9.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.9.0
  crypto: ^3.0.6

flutter:
  uses-material-design: true
